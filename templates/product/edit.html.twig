{% extends 'base.html.twig' %}

{% block title %}Modifier le produit{% endblock %}

{% block page_title %}Édition du produit{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto">
        <div class="mb-6 flex justify-between items-center">
            <a href="{{ path('app_product_index') }}" class="text-indigo-600 hover:underline text-sm flex items-center">
                ← Retour à la liste
            </a>
        </div>

        {{ include('product/_form.html.twig', {'button_label': 'Mettre à jour le produit'}) }}
    </div>
{% endblock %}